var LocalStorage = /** @class */ (function () {
    function LocalStorage() {
    }
    LocalStorage.prototype.getData = function () {
    };
    LocalStorage.prototype.storeData = function () {
    };
    return LocalStorage;
}());
export default LocalStorage;
